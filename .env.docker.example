# Copy this file to .env.docker and fill in the values, then run:
#   docker compose --env-file .env.docker up --build

# ── PostgreSQL ────────────────────────────────────────────────
POSTGRES_DB=pizzadb
POSTGRES_USER=pizza
POSTGRES_PASSWORD=change_me_strong_password

# ── FastAPI JWT ───────────────────────────────────────────────
SECRET_KEY=change-this-to-a-long-random-string-in-production

# ── First admin (created automatically on API startup) ────────
FIRST_ADMIN_EMAIL=admin@pizza.com
FIRST_ADMIN_PASSWORD=changeme123
FIRST_ADMIN_FULL_NAME=Administrator

# ── Supabase (still used by the storefront) ───────────────────
VITE_SUPABASE_URL=https://mcizinkxwwcxptfflpsi.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
VITE_SUPABASE_PROJECT_ID=mcizinkxwwcxptfflpsi
